require "spec_helper"

RSpec.describe Capistrano::Honeybadger::Sourcemaps do
  it "has a version number" do
    expect(Capistrano::Honeybadger::Sourcemaps::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
